% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tesz_smooth.R
\name{smooth.construct.tesz.smooth.spec}
\alias{smooth.construct.tesz.smooth.spec}
\alias{Predict.matrix.tesz.smooth}
\alias{smooth.info.tesz.smooth.spec}
\alias{Predict.matrix.tesz.factor}
\alias{Predict.matrix.tesz.check}
\title{Sum-to-zero factor interactions with tensor product smooths}
\usage{
\method{smooth.construct}{tesz.smooth.spec}(object, data, knots)

\method{Predict.matrix}{tesz.smooth}(object, data)

\method{smooth.info}{tesz.smooth.spec}(object)

\method{Predict.matrix}{tesz.factor}(object, data)

\method{Predict.matrix}{tesz.check}(object, data)
}
\arguments{
\item{object}{A smooth specification from \code{\link[mgcv:s]{mgcv::s()}} for the constructor,
or a fitted \code{tesz.smooth} object for the prediction method.}

\item{data}{A list or data frame containing the covariates and any \code{by}
variable required by the term.}

\item{knots}{Optional knots for the continuous margins.}
}
\value{
The constructor returns a \code{tesz.smooth} object containing the
design matrix, penalties, and information required for prediction.
The prediction method returns the corresponding model matrix.
}
\description{
Constructs a tensor product of two or more one-dimensional continuous
margins, with deviations that sum to zero across the levels of one factor.
}
\details{
Specify the term using \code{s(x, z, f, bs = "tesz")}, where \code{f} is a factor.
The position of the factor among the covariates does not matter. The smooth
is a tensor product despite being specified with \code{\link[mgcv:s]{mgcv::s()}}.

Supply continuous-margin options in \code{xt}: \code{k}, \code{bs}, \code{m}, and \code{xt} are
passed to \code{\link[mgcv:te]{mgcv::te()}} in the order of the continuous covariates. Defaults
are those of \code{te()} (cubic regression spline margins with dimension 5).
Specify marginal \code{k} and \code{m} inside \code{xt}, not in the outer \code{s()} call.

By default (\code{xt$shared = FALSE}), there is one smoothing parameter per
factor level and continuous direction. This follows the level-specific
default of mgcv's \code{sz} basis. These penalties apply to the original
level-specific surfaces, not the contrast surfaces. Set \code{xt$shared = TRUE}
for one smoothing parameter per continuous direction, shared across levels.

An orthonormal contrast basis absorbs the coefficient sum-to-zero constraint.
At every covariate combination, the deviations sum to zero across factor
levels with equal weights. The term includes factor offsets; adding the
same factor as a parametric effect would duplicate these offsets.

A separate factor supplied to \code{by} replicates the term using mgcv's standard
handling, preserving the sum-to-zero constraint within each represented
\code{by} level. Ordered factors omit the first level as usual. The \code{by} factor
must not encode the same grouping as the sum-to-zero factor, independently
of labels and level ordering. An internal categorical margin preserves
observed pairs of factor levels through model-frame subsetting, missing-value
removal, and discretization. The constructor checks equivalence using these
pairs, rather than comparing independently discretized factor columns.
This auxiliary margin has one constant basis function and introduces no
additional coefficients or penalties. No fitting wrapper is required.

Both ordinary and discrete \code{\link[mgcv:bam]{mgcv::bam()}} fitting are supported. Discrete
fitting stores the factor-contrast margin and each continuous margin
separately. Numeric \code{by} variables, outer \code{id} and \code{fx}, multiple sum-to-zero
factors, use as a tensor-product margin, and custom plotting are not
supported. The coefficient count is \code{(nlevels(f) - 1) * prod(k)}.
}
\examples{
set.seed(1)
dat <- data.frame(
  x = runif(300), z = runif(300),
  f = factor(rep(letters[1:3], 100))
)
dat$y <- sin(6 * dat$x) + as.integer(dat$f) * dat$z + rnorm(300)
fit <- mgcv::gam(
  y ~ te(x, z, k = c(4, 4)) +
    s(x, z, f, bs = "tesz", xt = list(k = c(4, 4))),
  data = dat, method = "REML"
)
predict(fit, newdata = dat[1:5, ])

# A separate factor can replicate the deviation surfaces:
# bam(y ~ g + te(x, z, by = g) +
#       s(x, z, f, bs = "tesz", by = g), data = dat, discrete = TRUE)
}
